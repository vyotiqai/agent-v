# Generated by the build (python3 screens7.py, python3 ds_build.py); reproducible, so not kept in git.
appmap/project/*.dc.html
ds/
__pycache__/
# The design canvas's runtime belongs to the canvas; fetch it (see README) rather than keep a copy.
runtime/
# Check output
out/
